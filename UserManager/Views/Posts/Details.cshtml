@model UserManager.Models.Post

@{
	ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
	<h4>Post</h4>
	<hr />
	<dl class="dl-horizontal">
		<dt>User</dt>
		<dd><a href="@Url.Action("Details", "Users", new { id = Model.UserId })">@Model.User.Username</a></dd>

		<dt>Title</dt>
		<dd>@Model.Title</dd>
	</dl>
</div>
<p>
	@Html.ActionLink("Edit", "Edit", new { id = Model.PostId }) |
	@Html.ActionLink("Back to List", "Index")
</p>